import React, { Component } from 'react';
import '../screen.less';

export default class Strategy extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <div>
        Strategy
      </div>
    );
  }
}
